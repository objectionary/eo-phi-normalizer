{⟦
  org() ↦ ⟦
    eolang() ↦ ⟦
      dataized(target) ↦ ⟦
        φ ↦ Φ̇.try(
          ξ.target,
          ⟦
            ex ↦ ∅,
            φ ↦ Φ̇.error(ξ.ex)
          ⟧,
          Φ̇.bytes(⟦ Δ ⤍ 01- ⟧)
        )
      ⟧,
      λ ⤍ Package
    ⟧,
    λ ⤍ Package
  ⟧
⟧}
